using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Consts : MonoBehaviour
{

    public enum Choice { Column, Row }
    public enum PlayerDisk { diskA, diskB }
    public enum PlayMode { PVP, PC, PCvPC }

}
